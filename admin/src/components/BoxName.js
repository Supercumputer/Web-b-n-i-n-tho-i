const BoxName = ({ title }) => {
    return (
        <div className="bg-[#fff] px-3 py-2 mb-3 rounded-sm border-l-4 border-yellow-300">
        <p className="font-semibold">{title}</p>
      </div>
    );
  };
  
  export default BoxName;
  